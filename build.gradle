/*
 * This build file was generated by the Gradle 'init' task.
 */

apply plugin: 'java'
apply plugin: 'java-library'

// In this section you declare where to find the dependencies of your project
repositories {
    jcenter()
}

dependencies {

    // https://mvnrepository.com/artifact/it.unibo.alice.tuprolog/tuprolog
    //implementation group: 'it.unibo.alice.tuprolog', name: 'tuprolog', version: '3.2.1'

    //TODO meglio avere un riferimento ad una libreria online (quella qui sopra non contiene tutte le classi necesarie)
    implementation fileTree(dir: 'src/main/resources', includes: '*.jar')

    // Use JUnit test framework
    testImplementation 'junit:junit:4.12'
}

